/**
 * Component-level frontend exports. This re-exports the runtime initializer so
 * other modules/tests can import from the module folder, mirroring the example
 * module structure.
 */
export * from '../../frontend';
